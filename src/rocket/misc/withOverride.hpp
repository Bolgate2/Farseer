#ifndef VAR_W_OVERRIDE_
#define VAR_W_OVERRIDE

namespace Rocket{
    /*
    const enum overrideFlags{
        NONE = 1 << 0,   // b0001
        OVERRIDE = 1 << 1,   // b0010
        OVERRIDEALL = 1 << 2 // b0100
    };

    
    template <typename T> class RocketParam {
        private:
            T _value;
            int _flags = overrideFlags::NONE;
        protected:
            virtual T value(); // this dictates the actual value that is gotten IF no overrides are present
        public:
            RocketParam<T>();
            RocketParam<T>(T value);

            // this function is the unoveridden value of this object
            T value();

            
            T get(); // this returns value if overrid
            void set(T val);
            void setOverride(int flags);
            void setOverride(bool override);
            void setOverrideAll(bool override);
    };
    */
}

#endif