#include "internalComponent.hpp"
#include <vector>

namespace Rocket{
    // these functions are pure virtual as the component vectors are not defined here
    /*
    std::vector<Component*> InternalComponent::components(){
        std::vector<Component*> castVec(_internalComponents.begin(), _internalComponents.end());
        return castVec;
    }
    std::vector<InternalComponent*> InternalComponent::internalComponents(){
        return _internalComponents;
    }
    InternalComponent* InternalComponent::findComponent(std::string id){
        return this;
    }
    InternalComponent* InternalComponent::addComponent(InternalComponent* component){
        return component;
    }
    void InternalComponent::removeComponent(InternalComponent* component){
        
    }
    */
}